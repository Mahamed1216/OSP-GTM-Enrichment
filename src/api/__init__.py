"""Internal API for SalesOS — process leads through the existing pipeline.

See src/api/server.py for the FastAPI app and src/api/processing.py for the
orchestration that reuses the existing import/enrichment/scoring/content code.
"""
