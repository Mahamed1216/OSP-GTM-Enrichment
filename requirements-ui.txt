# Streamlit UI dependencies (human-maintained input file), installed on top of
# requirements.txt.
#
# Dependency upper-bound GUARDRAILS — not a full reproducible lock.
#   - <next-major caps prevent a surprise major-version upgrade from breaking
#     the UI; current resolution is unchanged (no upgrades/downgrades).
#   - This does NOT lock transitive dependencies (minor/patch versions can
#     still change between installs).
#
# FOLLOW-UP: generate constraints.txt from the production/CI Python (likely
# 3.12 — not 3.14; see requirements.txt header for the command), then install:
#     pip install -r requirements-ui.txt -c constraints.txt
streamlit>=1.36.0,<2.0
pandas>=2.2.0,<4.0
nest_asyncio>=1.6.0,<2.0
